# ============================================
# DeepTutor School Deployment — Additional Dependencies
# ============================================
# Install on top of server.txt for multi-user school deployments:
#   pip install -r requirements/server.txt
#   pip install -r requirements/school.txt
#
# These packages enable:
#   - Alembic migrations (versioned schema upgrades)
#   - PostgreSQL driver for multi-user concurrency
#   - bcrypt password hashing
#   - SQLAlchemy (used by the user store and migrations)
#
# JWT signing uses only the Python stdlib, so no extra crypto package is
# required.

-r server.txt

# --- Schema migrations ---
alembic>=1.13.0
sqlalchemy>=2.0.0

# --- PostgreSQL drivers ---
# psycopg is the sync driver used by Alembic during migrations.
# asyncpg is used by the runtime app for non-blocking queries.
psycopg[binary]>=3.1.0
asyncpg>=0.29.0

# --- Authentication ---
bcrypt>=4.0.0
